# 本页资源清单验证记录

日期：2026-09-04。范围：仅编辑器前端及使用文档。

## 配置变化

- plmConfig增加fieldCodes、actionCodes，随原有JSON保存接口持久化、归档、复制和导出。
- 加载时合并现有字段/事件/Table绑定引用，兼容旧页面及缺失定义，不更改原绑定。
- 新页资源清单独立；字段按对象类型及关键字筛选，动作按用途和关键字筛选。
- 下方绑定选择限定为本页资源；禁用动作不可新增绑定，已使用资源不可直接移除。
- 无数据库迁移，无PLM、Widget或JPO修改；不改变真实执行权限。

## 已执行验证

- `node --test tests/page-resources.test.mjs`：3项通过。覆盖实例隔离、旧配置恢复与去重、输入不变、未绑定项/缺失引用保留和序列化往返。
- `pnpm build`：TypeScript检查及生产构建通过，仍有AMIS第三方循环依赖、eval及包体积警告。
- 浏览器：原竞品BOM V6自动恢复3个字段、2个动作；已绑定项移除按钮禁用。
- 浏览器：选入CURRENT后，清单由3变4且字段绑定下拉同步增加；移除后恢复原样、未保存标记消失，原页面没有提交保存。
- 浏览器：新产品演示页字段/动作清单均为0，3种新增绑定入口均禁用，证明不会沿用前页资源。
- 浏览器：字段搜索“零件”只保留零件子类型；动作按方法名getCompetitiveBOMList搜索只保留查询动作。
- API临时页LC_VERIFY_RESOURCES_20260904：POST/GET验证清单一致，PUT产生V2；验证完成已软删除，历史仍保留。未改写现有业务页面。

## 边界

- 候选筛选在已加载的公共库上执行，非服务端分页；未来字段规模很大时再扩展按需加载。
- 动作库目前没有对象类型元数据，故动作按用途筛选，不推断JPO适用类型。
- 本次仅归档资源编码清单，不实现公共定义版本锁定、发布快照或真实PLM权限判断。
